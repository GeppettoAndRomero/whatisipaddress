require 'spec_helper'

describe Whatisipaddress do
  it 'has a version number' do
    expect(Whatisipaddress::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
